# 변경 이력

이 문서는 이 저장소의 주요 변경 사항을 한글로 정리한 파일입니다.
아래 내용은 현재 로컬 Git 이력 기준으로 재구성했습니다.

## 2026-05-13

### PostgreSQL 전환과 날짜 기반 루틴 저장

- PC2 영속 계층을 PostgreSQL 중심 구조로 전환했습니다.
- PostgreSQL Docker 실행 구성을 추가하고 기본 호스트 포트를 `5430`으로 정리했습니다.
- SQLite fallback 흔적을 제거하고 PostgreSQL 전용 운영 기준으로 문서와 설정을 정리했습니다.

### 프로필 루틴 확장

- 프로필 기반 주간 루틴 생성 시 `weekly_routine`만 만드는 것이 아니라, 각 day를 다시 상세 생성하는 흐름으로 확장했습니다.
- `how_to`, `tips` 필드를 운동 item에 추가해 루틴 설명을 더 풍부하게 만들었습니다.
- `routine_id`, `start_date`, `scheduled_dates`를 포함한 일정 메타 정보를 함께 반환하도록 정리했습니다.
- `GET /api/routine/profile/{user_id}/day?target_date=YYYY-MM-DD` 경로를 추가해 특정 날짜 루틴을 조회할 수 있게 했습니다.

### 연동 문서 보강

- `PC1_PC3_INTEGRATION.md`를 추가해 PC1이 PC3에 보내야 하는 루틴 생성/조회 명세를 분리했습니다.
- `PC3_PC2_INTEGRATION.md`에 요청/응답 DTO 예시를 추가했습니다.
- `CHANGELOG.md`, `FLOW_CHANGES.md`, 루트 `SKILL.md`를 추가해 문서 관리 규칙을 명시했습니다.
- PC1, PC3, PC2 연동 문서에 `restricted_body_parts` 의미, 입력 범위 제한, 추가 필드 제외 규칙을 보강했습니다.

## 2026-05-12

### 프로필 기반 루틴 생성 도입

- `POST /api/routine/profile` 기반의 프로필 루틴 생성 기능을 도입했습니다.
- 주간 요약, 주간 focus, day별 운동 목록, 주의사항을 포함하는 루틴 응답 구조를 추가했습니다.
- 프로필 루틴은 primary LLM 전용 경로로 설계하고, 실패 시 `503`을 반환하도록 분리했습니다.

### 루틴 저장과 검증 강화

- 루틴 저장 로직을 추가해 생성 결과를 DB에 기록할 수 있게 했습니다.
- 루틴 응답에서 허용 운동 타입 제한을 반영했습니다.
- 루틴 생성 타임아웃 기본값을 상향해 응답 잘림 가능성을 낮췄습니다.
- 프로필 루틴 응답 검증을 강화해 빈 응답, 초과 일수, 잘못된 운동 이름 같은 경우를 더 엄격히 막았습니다.

### 운영/문서 정리

- fallback vLLM 표준 실행 기준을 문서와 `.env.example`에 반영했습니다.
- PC3 요청 변수 의미와 셀프테스트 문서를 보강했습니다.
- 스모크 테스트 세션 식별자 충돌을 방지하도록 테스트 스크립트를 정리했습니다.
- 불필요한 파일과 환경 예시를 정리하고, 셀프테스트 결과 산출물 정리를 진행했습니다.

## 2026-05-11

### 연동 및 테스트 문서 확장

- PC3 연동 문서를 더 구체화했습니다.
- 셀프테스트 절차와 결과 문서를 추가해 서버 기동 후 검증 흐름을 문서화했습니다.

## 2026-05-10

### 초기 구성

- PC2 Coach API 기본 구조를 추가했습니다.
- FastAPI 서버, 스키마, DB 계층, LLM 호출 계층, 프롬프트 구성 계층, 운동 신호 해석 계층을 초기 구성했습니다.
- `/api/coach/generate`, baseline 저장/조회, 문서, 실행 스크립트, fallback vLLM Docker 구성을 포함한 초기 저장소를 만들었습니다.

### 코칭 응답 안정화

- LLM 응답 파싱과 fallback 흐름을 안정화했습니다.
- raw 응답 파싱, local fallback, compact 응답 생성, 문서와 테스트를 함께 보강했습니다.

